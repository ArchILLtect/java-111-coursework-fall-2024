/**
 *  Test drives the classmate class.
 *
 *@author    Nick Hanson
 */
public class ClassmateListTestDrive {
    /** Instantiate an Classmate object and call run on it.
     * 
     * @param args command line arguments (not used here)
     */

    public static void main(String[] args) {

        // Instantiate a new Classmate object
        ClassmateList list = new ClassmateList();
        // Call the run method
        list.run();
    }
}
